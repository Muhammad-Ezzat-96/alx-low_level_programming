#include "main.h"
#include <stdio.h>
/**
  *_isdigit - checks for a number whether is digit or not
  *@c: inter to be checked
  *Return: always 0
  */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
