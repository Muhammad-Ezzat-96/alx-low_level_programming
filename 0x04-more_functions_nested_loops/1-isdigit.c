#include "main.h"
#include <stdio.h>
/**
  *isdigit - checks for a number whether is digit or not
  *Discription: a function that checks for a digit (0 through 9)
  *Return: always 0
  */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
