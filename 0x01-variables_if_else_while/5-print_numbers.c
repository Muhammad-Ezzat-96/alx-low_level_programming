#include <stdio.h>
/**
  *main - Entry point
  *Discription: all base 10 single digit numbers
  *Return: always 0
  */
int main(void)
{
	int digit = 0;

	for (digit = 0; digit < 10; digit++)
	{
		printf("%d\n", digit);
	}
	return (0);
}
